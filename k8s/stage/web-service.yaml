apiVersion: v1
kind: Service
metadata:
  name: festadelallum-web-stage
  labels:
    app: festadelallum-web-stage
  namespace: stage
spec:
  selector:
    app: festadelallum-web-stage
  type: ClusterIP
  ports:
    - name: http
      port: 80
